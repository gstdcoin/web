# GSTD Token Site Update
I added the `Ecosystem` component just below the Hero, integrating direct links for `https://app.gstdtoken.com/`, `https://monitor.gstdtoken.com/`, and `https://chat.gstdtoken.com/`. 

- Re-wrote the CTA inside the `Hero` section to map to the new "Platform" (`app.gstdtoken.com`) using golden styling.
- Repurposed the generic telegram "Community" button into the "AI Chat" link to make it more obvious.
- Cleaned up the `Navbar` to include "Platform", "Chat" and "Network" linking to the three sub-domains directly, removing the redundant "Buy" and "Token" links along with `docs` and `legal`. 
- Repointed the bottom page CTA buttons.
- Translated to both English, mapped over `copy.en.ts`, and `copy.ru.ts`.
- Re-composed the docker project, it is fully deployed now.
